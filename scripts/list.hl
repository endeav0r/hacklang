li = list()
list_append(li, "one")
list_append(li, "two")
list_append(li, "three")

it = iter(li)
print("size " + str(list_size(li)) + "\n")
while iter_continue(it)
    print(str(iter_next(it)) + "\n")
end